{
  "name": "am-marketing",
  "version": "1.0.0",
  "private": true,
  "description": "AM Marketing — requisicoes de materiais do Marketing do Grupo Alcina Maria",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "sync:shared": "node scripts/sync-shared.mjs",
    "install:all": "npm --prefix frontend ci && npm --prefix backend ci",
    "dev:front": "npm --prefix frontend run dev",
    "dev:back": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "typecheck": "npm --prefix frontend run typecheck && npm --prefix backend run typecheck",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run lint",
    "test": "npm --prefix frontend run test",
    "seed": "npm --prefix backend run seed"
  }
}
