{
  // Os scripts (seed) rodam via `tsx` e ficam fora do build de producao, mas
  // continuam entrando no `npm run typecheck`.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": ".",
    "noEmit": true
  },
  "include": ["scripts/**/*.ts", "src/**/*.ts"]
}
